image: ubuntu:16.04

stages:
 - build

before_script:
  - apt update
  - apt install meson valac libgtk-3-dev libgusb-dev libcolord-dev libpackagekit-glib2-dev libwebp-dev libsane-dev gettext itstool

build-simple-scan:
  stage: build
  script:
    - meson _build .
    - ninja -C _build install
